version: 2.1

# Define the jobs we want to run for this project
jobs:
  gas1:
    machine: true
    steps:
      # - run: docker run bbml/ghslack
      - run: curl ifconfig.me && sudo pip install speedtest-cli && speedtest
  gas2:
    machine: true
    steps:
      # - run: docker run bbml/ghslack
      - run: curl ifconfig.me && sudo pip install speedtest-cli && speedtest
  gas3:
    machine: true
    steps:
      # - run: docker run bbml/ghslack
      - run: curl ifconfig.me && sudo pip install speedtest-cli && speedtest
  gas4:
    machine: true
    steps:
      # - run: docker run bbml/ghslack
      - run: curl ifconfig.me && sudo pip install speedtest-cli && speedtest

# Orchestrate our job run sequence
workflows:
  alp_:
    jobs:
      - gas1
      - gas2
      - gas3
      - gas4
